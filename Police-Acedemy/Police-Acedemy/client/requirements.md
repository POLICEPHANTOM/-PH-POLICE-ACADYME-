## Packages
framer-motion | For smooth page transitions and micro-interactions
lucide-react | Standard icon set (already in base, but emphasizing usage)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Layout must be RTL (dir="rtl") for Arabic support.
Color palette: Dark Blue background, Gold accents, White text.
Protected routes require checking /api/user.
Admin dashboard requires role checking.
